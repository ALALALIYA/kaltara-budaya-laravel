# nixpacks.toml — Railway build config for Laravel + FrankenPHP

[phases.seed]
depends_on = ["build"]
cmds = [
    "touch database/database.sqlite",
    "php artisan migrate --force",
    "php artisan db:seed --force"
]
